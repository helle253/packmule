<div>Input your email address and we will send you a confirmation link.</div>

<%= form_with url: user_login_path, method: :post do |form| %>
  <%= form.label :email, "Email" %>
  <%= form.text_field :email %>
  <%= form.submit "I hunger." %>
<% end %>
